# Dependencies
/vendor/
composer.lock
node_modules/
package-lock.json
yarn.lock

# Environment variables (never commit secrets)
.env
.env.local
.env.*.local

# Security scan artifacts (never commit — may contain finding context)
gitleaks-report.json

# IDE & Editor
.vscode/
.idea/
*.swp
*.swo
*~
.DS_Store
.sublime-project
.sublime-workspace

# Build & Artifacts
/build/
/dist/
*.log
*.tmp

# Test coverage
/coverage/
/build/coverage/
build/clover.xml

# Database
*.db
*.sqlite
*.sqlite3

# OS
Thumbs.db

# Temporary files
*.bak
.temp/

# Project-specific
# NOTE: .keel/state/ and .keel/memory/ are deliberately COMMITTED — git history
# is the pipeline's audit trail. Never add them here.
.keel/watch/

# Generated files (should be committed for framework, but not for user projects)
# Keep these in keel:
# - /docs/requirements/
# - /docs/design/
# - /docs/deployment/
# - /docs/security/
# - /docs/brainstorms/

# Test/browser scratch output
.playwright-mcp/
test-results/
playwright-report/
